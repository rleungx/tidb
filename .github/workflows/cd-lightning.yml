name: Lightning-CD

on:
  push:
    branches: ["release-7.1-keyspace"]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-arm64:
    name: Build ARM64 Image
    runs-on: arm64
    steps:
      - name: Check out Code
        uses: actions/checkout@v2
      - name: Build and Push Images
        uses: docker/build-push-action@v2
        with:
          context: .
          platforms: "linux/arm64"
          file: br/pkg/lightning/Dockerfile
          push: true
          secrets: |
            GIT_AUTH_TOKEN=${{ secrets.GIT_ACTION_BOT }}
          build-args: |
            GITHUB_TOKEN=${{ secrets.GIT_ACTION_BOT }}
          tags: hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}-arm64
  build-amd64:
    name: Build AMD64 Image
    runs-on: amd64
    steps:
      - name: Check out Code
        uses: actions/checkout@v2
      - name: Build and Push Images
        uses: docker/build-push-action@v2
        with:
          context: .
          platforms: "linux/amd64"
          file: br/pkg/lightning/Dockerfile
          push: true
          secrets: |
            GIT_AUTH_TOKEN=${{ secrets.GIT_ACTION_BOT }}
          build-args: |
            GITHUB_TOKEN=${{ secrets.GIT_ACTION_BOT }}
          tags: hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}-amd64
  multi-arch:
    name: Create Multi-Architecture Image
    runs-on: amd64
    needs: ["build-arm64", "build-amd64"]
    steps:
      - name: create-manifest
        run: |
          docker manifest create hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }} \
          --amend hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}-amd64 \
          --amend hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}-arm64
      - name: push-manifest
        run: |
          docker manifest push hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}
      - name: set-additional-tags
        if: ${{ github.ref == 'refs/heads/release-7.1-keyspace' && github.event_name == 'push' }}
        run: |
          docker manifest create hub.pingcap.net/keyspace/tidb-lightning:v7.1.0 \
          --amend hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}-amd64 \
          --amend hub.pingcap.net/sunxiaoguang/serverless/tidb:lightning-${{ github.sha }}-arm64
          
          docker manifest push hub.pingcap.net/keyspace/tidb-lightning:v7.1.0
